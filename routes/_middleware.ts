import { withLive } from "$live/live.ts";

export const handler = withLive({
  siteId: 279,
  site: "deus-patria e familia",
  domains: ["deus-patria-e-familia-deco.deno.dev", "lucis-dpf-deco.deno.dev"],
});
